const CreateStatsDiv = (totalRecords, totalUsers) => {
  return (
    <div>
      <p>Total Records: {totalRecords}</p>
      <p>Total Users: {totalUsers}</p>
    </div>
  );
};

export default CreateStatsDiv;
